import { createButton } from './button'
import { createInput } from './input'
import { createCheckbox } from './checkbox'
import { createLabel } from './label'
export { createButton, createInput, createCheckbox, createLabel }
